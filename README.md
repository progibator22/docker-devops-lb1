# 🐳 Отчет по лабораторной работе №1: Docker: создание и управление контейнерами

## 1. Сведения об исполнителях

| Роль | ФИО | Группа | Учебное заведение |
| :--- | :--- | :--- | :--- |
| Исполнитель 1 | Самигуллин Д.Р. | ПИ-432Б | УУНиТ |
| Исполнитель 2 | Мухиев Г.А. | ПИ-432Б | УУНиТ |

---

## 2. Цель работы

Целью работы является изучение работы с программным обеспечением Docker для автоматизации развертывания и управления приложениями в средах с поддержкой контейнеризации. В ходе работы необходимо собрать образ веб-приложения (`app.py`), обеспечить его взаимодействие с базой данных PostgreSQL и развернуть с помощью Docker Compose.

---

## 3. Ход выполнения работы (Описание методики)

Работа выполнялась на чистой виртуальной машине с ОС Ubuntu. Все необходимые файлы (`app.py`, `requirements.txt`, `Dockerfile`, `docker-compose.yml`) были созданы в рабочей директории.

### 3.1. Установка и подготовка

1.  Были изучены теоретические основы работы с Docker и Docker Compose.
2.  Выполнена установка необходимых пакетов, включая Docker Engine (CE) и docker-compose.
    * Добавлен GPG-ключ и настроен официальный репозиторий Docker.
    * Пользователь добавлен в группу `docker` для работы без `sudo`.

### 3.2. Создание образа и одиночный запуск

1.  Создан `Dockerfile` для веб-приложения, основанный на образе Python.
    * В `Dockerfile` предусмотрена установка зависимостей (`Flask`, `psycopg2-binary`) и проброс порта `1234`.
2.  Собран образ Docker с тегом `my-lab-app` на основе созданного `Dockerfile`.
    ```bash
    docker build -t my-lab-app .
    ```
3.  Запущен контейнер с приложением в изолированном режиме.
    ```bash
    docker run -d -p 1234:1234 --name test-app my-lab-app
    ```
4.  Проверена доступность приложения с хост-машины по адресу `http://localhost:1234/`.
    * Получено ожидаемое сообщение: **"Извините, но подключения к базе не случилось :("** (так как база данных еще не была развернута).
    * Контейнер `test-app` был остановлен и удален.

### 3.3. Развертывание с Docker Compose

1.  Написан файл `docker-compose.yml` (с использованием версии `2.4` из-за особенностей установки `docker-compose`) для одновременного развертывания двух сервисов: `web` (приложение) и `db` (PostgreSQL).
    * Обеспечена связь сервисов через внутреннюю сеть Docker.
    * Проброшен порт `1234` для доступа к веб-приложению.
    * Предусмотрено использование тома (`volumes`) для сохранения данных PostgreSQL, а также зависимости (`depends_on`) для контроля порядка запуска.
2.  Запущен стек через Docker Compose.
    ```bash
    docker-compose up -d
    ```
3.  Проведена финальная проверка успешности подключения к базе данных.
    * Получено сообщение: **"Привет Докер и postgres!"**, что подтверждает успешное взаимодействие между контейнером приложения и контейнером базы данных.

---

## 4. Файлы проекта

* `app.py`: Исходный код веб-приложения на Flask.
* `requirements.txt`: Список зависимостей Python.
* `Dockerfile`: Инструкции по сборке образа приложения.
* `docker-compose.yml`: Конфигурация для запуска приложения и PostgreSQL.
